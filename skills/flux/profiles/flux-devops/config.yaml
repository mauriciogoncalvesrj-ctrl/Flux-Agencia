# Flux DevOps — Configuração Hermes, debugging, scripts, infra
model:
  default: glm-5.1
  provider: opencode-go

agent:
  max_turns: 40
  reasoning_effort: high
  gateway_timeout: 1800
  tool_use_enforcement: auto

toolsets:
- terminal
- file
- web
- memory
- skills

skills:
  auto_load:
  - flux-ops
